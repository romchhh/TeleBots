import NotFound from '../ui/NotFound'

export const metadata = {
  title: 'TeleBots - Сторінку не знайдено',
  description: 'Сторінку не знайдено',
}

export default function NotFoundPage() {
  return <NotFound />
} 